name: Pull Request

on:
  pull_request:

jobs:
  build:
    strategy:
      matrix:
        GPIO_ONLY: [false, true]
    uses: ./.github/workflows/build.yml
    with:
      GPIO_ONLY: ${{ matrix.GPIO_ONLY }}
